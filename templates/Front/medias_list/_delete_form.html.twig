<form method="post" action="{{ path('front_app_medias_list_delete', {'id': medias_list.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ medias_list.id) }}">
    <button class="btn">Delete</button>
</form>
