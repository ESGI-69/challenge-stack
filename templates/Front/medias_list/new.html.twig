<!DOCTYPE html>

<title>New MediasList</title>

{% block body %}
    <h1>Create new MediasList</h1>

    {{ include('medias_list/_form.html.twig') }}

    <a href="{{ path('app_medias_list_index') }}">back to list</a>
{% endblock %}
