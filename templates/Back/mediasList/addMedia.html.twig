{% extends 'base_back.html.twig' %}

{% block title %}BeatHub | Ad media to a list{% endblock %}

{% block body %}
    <h1>Add a media to : {{ mediasList.title }} </h1>

    {% if linkedArtist == false %}
      <p> You must be linked to an artist to create a medias list, ask to your manager.</p>
    {% else %}
      {{ include('Back/mediasList/_form.html.twig') }}
    {% endif %}


    <a href="{{ path('admin_app_mediaslist_index') }}">back to list</a>
{% endblock %}
