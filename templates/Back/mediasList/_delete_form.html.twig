<form method="post" action="{{ path('admin_app_mediaslist_delete', {'id': mediasList.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mediasList.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
