{% extends 'base_back.html.twig' %}

{% block title %}Add artist to a groupe{% endblock %}

{% block body %}
    {{ dump(user) }}
    {{ form_start(form) }}
      {{ form_rest(form) }}
      <input type="submit" value="Modifier">
    {{ form_end(form) }}
{% endblock %}
